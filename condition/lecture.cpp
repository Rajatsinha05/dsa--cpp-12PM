#include <iostream>
using namespace std;

int main()
{
    int a, b;
    cin >> a >> b;
    if (a > b)
    {
        cout << "A is bigger than B";
    }
    else
    {
        cout << "A is smaller than B";
    }

    return 0;
}